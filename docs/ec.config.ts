import type { AstroExpressiveCodeOptions } from 'astro-expressive-code'

const config: AstroExpressiveCodeOptions = {
	plugins: [
		{
			name: 'test 244',
			baseStyles: () => `color: red;`,
		},
	],
}

export default config
